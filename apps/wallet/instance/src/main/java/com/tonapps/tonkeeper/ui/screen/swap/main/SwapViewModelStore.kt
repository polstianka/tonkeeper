package com.tonapps.tonkeeper.ui.screen.swap.main

import androidx.lifecycle.ViewModelStoreOwner

class SwapViewModelStore {
    companion object {
        fun getOwnerForId(id: String): ViewModelStoreOwner {
            error("")
        }
    }
}